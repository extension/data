<ul class="nav">
	<%= link_to("eXtension Data",root_path,:class=>'brand')%>
</ul>

<!-- Core Navigation -->
<%- if !@hide_navbar%>
<ul class="nav">
  <li class="divider-vertical"></li>
  
	<%= form_tag(search_pages_path, { :class => 'navbar-search pull-left' }) do -%>
    <%= text_field_tag :q, params[:q], {:class => 'search-query', :placeholder => 'Find page (title, page id, or node id)', :autocomplete => 'off'} -%>
	<%- end -%>
  <li class="divider-vertical"></li>
  
  
	<%= nav_item(pages_path,'Pages') %>
	<%= nav_item(nodes_path,'Nodes') %>
	<%= nav_item(groups_pages_path,'Groups') %>
</ul>
<%- end -%>



<ul class="nav pull-right">
	<%- if @currentcontributor -%>
	<li class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= @currentcontributor.name %><b class="caret"></b></a>
		<ul class="dropdown-menu">
			<%= nav_item(deploys_path(:coder => @currentcontributor.id),'Your Data') %>  
			<li class="divider"></li>
			<%= nav_item(logout_path,'Sign out') %>
		</ul>
	</li>
	<%- else -%>
    <%- # Nothing for now -%>
	<%- end -%>	
</ul>

